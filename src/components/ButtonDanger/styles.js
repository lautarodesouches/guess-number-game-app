import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonDanger: {
    backgroundColor: 'crimson',
  },
  buttonDanger__text: {
    color: '#fff',
    textAlign: 'center',
    fontSize: 17,
  },
});